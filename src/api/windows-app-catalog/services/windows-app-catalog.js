'use strict';

/**
 * windows-app-catalog service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::windows-app-catalog.windows-app-catalog');
